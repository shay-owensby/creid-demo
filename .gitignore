# Local Claude Code settings
.claude/settings.local.json

# Report exports (private — never commit)
PROPERTY-*
*.pdf

# macOS
.DS_Store
.vercel

# Collected event leads (PII — never commit)
leads/leads.md
leads/leads.jsonl
